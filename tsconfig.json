{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "baseUrl": ".",
    "paths": {
      // 💥 关键修正：所有以 @/ 开头的路径都映射到 ./src/*
      // 这包含了 @/lib/*, @/components/*, @/types/*
      "@/*": ["./src/*"] 
      
      // 注意：现在不需要额外的 @/lib/* 映射了
      // 如果您的 types 文件夹也在 src 内部 (src/types/prompt.ts), 
      // 那么它也已经被上面的 @/* 包含了。
    }
  },
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    ".next/dev/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules"]
}